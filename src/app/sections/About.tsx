import React from "react";
import AnimatedButton from "../components/AnimatedButton";

export default function About() {
    return (
        <section id="about" className="backdrop-blur-sm min-h-screen">
            <AnimatedButton />
            About Page Content
        </section>
    );
}
